
vec is a struct
structs are like types that have a collection of things of length unknown at compile time
traits are constraints (requirements for an object to satisfy an interface)
cannot iterate over a vec but can call iter() on it with zero cost abstraction

look up optional types (eg Some()
